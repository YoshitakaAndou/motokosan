import 'package:flutter/material.dart';

class UnConnect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("圏外みたいです！")),
    );
  }
}
